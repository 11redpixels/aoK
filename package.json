{
  "name": "aok",
  "version": "1.0.0",
  "description": "Agent Operating Kernel — Autonomous Software Maintenance Engine",
  "main": "dist/aok.js",
  "bin": {
    "aok": "./dist/aok.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --no-warnings --experimental-strip-types tests/run-cli-checks.js && node --no-warnings --test --experimental-strip-types tests/*.test.js"
  },
  "keywords": [
    "aok",
    "autonomous",
    "testing",
    "playwright",
    "self-healing"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "commander": "^14.0.3",
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^25.6.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.2"
  }
}
