export function hotspot() {
  return [
    'This file exists to give the exploration engine a concrete target.',
    'The fixture keeps the source small and deterministic.',
    'Historical regressions are injected through evolution memory in the test.',
  ].join(' ');
}
